// test-integration-simple.js
// ПРОСТОЙ ТЕСТ ИНТЕГРАЦИИ БЕЗ ЗАВИСИМОСТЕЙ

const SimpleTestManager = require('./simple-test-manager');

async function main() {
    console.log('🚀 ЗАПУСК ПРОСТОГО ТЕСТА ИНТЕГРАЦИИ');
    console.log('='.repeat(60));
   
    const testManager = new SimpleTestManager();
   
    try {
        // Запускаем все тесты
        const results = await testManager.runAllTests();
       
        console.log('\n' + '='.repeat(60));
        console.log('✅ ТЕСТИРОВАНИЕ ЗАВЕРШЕНО!');
       
        // Краткий вывод
        const passed = results.filter(r => r.success).length;
        const failed = results.filter(r => !r.success).length;
       
        console.log(`\n📊 ИТОГО: ${passed} пройдено, ${failed} провалено`);
       
        if (failed === 0) {
            console.log('\n🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ! Система готова к работе.');
        } else {
            console.log('\n⚠️ Есть проблемы. Проверьте проваленные тесты.');
        }
       
    } catch (error) {
        console.error('❌ КРИТИЧЕСКАЯ ОШИБКА:', error);
        process.exit(1);
    }
}

// Запуск
main().catch(console.error);
